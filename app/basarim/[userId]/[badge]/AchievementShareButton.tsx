"use client";import { Share2 } from "lucide-react";export default function AchievementShareButton({badge,username}:{badge:string;username:string}){return <button onClick={async()=>{const data={title:`${badge} | OtoSöz`,text:`@${username} OtoSöz'de “${badge}” rozetini kazandı.`,url:location.href};try{if(navigator.share)await navigator.share(data);else await navigator.clipboard.writeText(`${data.text} ${data.url}`);}catch{}}}><Share2/> Başarımı paylaş</button>}